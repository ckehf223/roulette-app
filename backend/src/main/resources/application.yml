spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/roulette?characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: root
    password: song1074

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true

mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.spring.board
  configuration:
    map-underscore-to-camel-case: true


